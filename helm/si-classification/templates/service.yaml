apiVersion: v1
kind: Service
metadata:
  name: {{ include "si-classification.fullname" . }}
  labels:
    {{- include "si-classification.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "si-classification.selectorLabels" . | nindent 4 }}
